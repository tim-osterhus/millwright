# Security policy

Millwright is pre-alpha and is not ready for sensitive or production workloads.
The inherited harness can execute model-generated Python and shell commands with
the current user's permissions; its process boundaries are not a security
sandbox.

Do not include secrets, private vulnerability details, or live credentials in
public issues. A private reporting channel will be documented before the first
supported release.
